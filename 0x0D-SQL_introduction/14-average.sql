-- MySQL server
-- should computes the score average of all records in the table second_table
-- should the result to be average by colume name
-- should the database name will be passed as an argument

SELECT AVG(score) AS average FROM second_table;
