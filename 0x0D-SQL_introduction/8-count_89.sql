-- MySQL server
-- should displays the number of records with id = 89 in the table first_table
-- should the database name will be passed as an argument

SELECT COUNT(*) FROM first_table WHERE id = 89;
